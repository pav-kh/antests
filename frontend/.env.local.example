# Local dev: hit the backend directly on :8000.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# --- Production (Railway) ---
# NEXT_PUBLIC_API_BASE=/api          # client calls /api/* (same origin)
# BACKEND_URL=https://<backend>.up.railway.app   # rewrite target (next.config.mjs)
