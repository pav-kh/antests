# Backend API origin. REQUIRED for local dev (frontend:3000 -> backend:8000).
# If unset, the client defaults to same-origin ("") — correct for a production
# build served behind a reverse proxy, but it will NOT reach a separate backend.
NEXT_PUBLIC_API_BASE=http://localhost:8000
